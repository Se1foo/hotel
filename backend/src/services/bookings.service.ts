import type { Booking } from '../models/booking.model';

export function getPlannedBookings(): Booking[] {
  return [];
}

export function getBookingsRoadmapMessage(): string {
  return 'Bookings are planned for a later phase of the hotel application.';
}
